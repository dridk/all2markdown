[package]
name = "all2markdown-python"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "all2markdown"
crate-type = ["cdylib"]

[dependencies]
all2markdown-core = { path = "../all2markdown-core" }
pyo3 = { version = "0.23", features = ["extension-module"] }
