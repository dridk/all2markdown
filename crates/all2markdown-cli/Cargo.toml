[package]
name = "all2markdown-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
all2markdown-core = { path = "../all2markdown-core" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[dev-dependencies]
tempfile = "3.27.0"
serde_json = "1.0.151"
